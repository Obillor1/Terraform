This sis passed directly into the configuration file order than the variable.tf file.

Local {
  ami  = "ami-053b0d53c279acc90"
  type = "t3.medium"

  tags = {
    Name= "Oso-group"
    Env = "Development"
}
}


After this has been added, go to the config dependecies and add the key | value as;
++++++++++++
EC2 Instance
++++++++++++
ami             = "${local.ami}"
instance_type   = "${local.type}" 
